import typescriptLintOnlyConfig from '../typescript-lint-only.js';

/**
 * Mozayk ESLint configuration preset for TypeScript (lint only).
 *
 * @type {import("eslint").Linter.Config[]}
 */
export default [...typescriptLintOnlyConfig];
